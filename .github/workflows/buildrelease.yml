name: Build Release
on: 
  workflow_dispatch
jobs:
  # BuildWindows:
  #   runs-on: windows-latest
  #   steps:
  #     - uses: actions/checkout@v2
  #     - uses: actions/setup-python@v3
  #       with:
  #         python-version: '3.10'
  #         architecture: 'x64'
  #     - run: choco install innosetup
  #     - run: python -m pip install setuptools wheel
  #     - run: python -m pip install -U -r requirements.txt
  #     - working-directory: ./packaging
  #       run: ./windows.cmd
  #     - uses: actions/upload-artifact@v2
  #       with:
  #         name: dist-windows
  #         path: ./packaging/dist/*
  #         retention-days: 1
  # BuildMacOS:
  #   runs-on: macos-latest
  #   steps:
  #     - uses: actions/checkout@v2
  #     - uses: actions/setup-python@v3
  #       with:
  #         python-version: '3.10'
  #         architecture: 'x64'
  #     - run: python -m pip install setuptools wheel
  #     - run: python -m pip install -U -r requirements.txt
  #     - working-directory: ./packaging
  #       run: ./macos.sh
  #     - uses: actions/upload-artifact@v2
  #       with:
  #         name: dist-macos
  #         path: ./packaging/dist/*
  #         retention-days: 1
  BuildLinux:
    runs-on: ubuntu-latest
    container: python:3.10.12-slim-buster
    steps:
      - uses: actions/checkout@v2
      - run: apt-get update
      - run: apt-get install -y wget binutils file libglib2.0-0
      - run: python -m pip install requests
      - run: python -m pip install setuptools wheel
      - run: python -m pip install -U -r requirements.txt
      - working-directory: ./packaging
        run: ./linux.sh
      - uses: actions/upload-artifact@v2
        with:
          name: dist-linux
          path: ./packaging/dist/*
          retention-days: 1